-r requirements.txt

# Test runner + async support
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0

# Linting + formatting
ruff==0.4.10

# Test HTTP client (used by FastAPI TestClient)
httpx==0.27.0
